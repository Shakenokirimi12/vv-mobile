///
/// HybridVoicevoxSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `VoicevoxModelInfo` to properly resolve imports.
namespace margelo::nitro::voicevox { struct VoicevoxModelInfo; }
// Forward declaration of `DownloadResult` to properly resolve imports.
namespace margelo::nitro::voicevox { struct DownloadResult; }

#include <NitroModules/Promise.hpp>
#include "VoicevoxModelInfo.hpp"
#include <vector>
#include <string>
#include "DownloadResult.hpp"
#include <NitroModules/ArrayBuffer.hpp>
#include <optional>

namespace margelo::nitro::voicevox {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `Voicevox`
   * Inherit this class to create instances of `HybridVoicevoxSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridVoicevox: public HybridVoicevoxSpec {
   * public:
   *   HybridVoicevox(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridVoicevoxSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridVoicevoxSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridVoicevoxSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<void>> initialize(double maxConcurrentDownloads) = 0;
      virtual std::shared_ptr<Promise<std::vector<VoicevoxModelInfo>>> listModels() = 0;
      virtual std::string getTermsURL() = 0;
      virtual void acceptLicense(const std::string& modelId) = 0;
      virtual bool isLicenseAccepted(const std::string& modelId) = 0;
      virtual std::shared_ptr<Promise<void>> downloadModel(const std::string& id) = 0;
      virtual std::shared_ptr<Promise<std::vector<DownloadResult>>> downloadModels(const std::vector<std::string>& ids) = 0;
      virtual std::shared_ptr<Promise<std::vector<DownloadResult>>> downloadAllModels() = 0;
      virtual std::shared_ptr<Promise<std::shared_ptr<ArrayBuffer>>> synthesis(const std::string& text, const std::string& modelId, std::optional<double> styleId) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "Voicevox";
  };

} // namespace margelo::nitro::voicevox
